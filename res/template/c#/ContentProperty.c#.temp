     //${PROPERTY_NAME}
    private ${DATA_TYPE} _${PROPERTY};
${SEPARATOR}
    /**
     * ${PROPERTY_NAME}
     *
     * @remark ${REMARK}
     * @returns {${DATA_TYPE}}
     */
    public ${DATA_TYPE} ${PROPERTY} {
		get{return _${PROPERTY};}
    }