     //${PROPERTY_NAME}
    private _${PROPERTY}: ${DATA_TYPE};
${SEPARATOR}
    /**
     * ${PROPERTY_NAME}
     *
     * @remark ${REMARK}
     * @returns {${DATA_TYPE}}
     */
    get ${PROPERTY}(): ${DATA_TYPE} {
        return this._${PROPERTY};
    }