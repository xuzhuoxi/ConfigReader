	     //${PROPERTY_NAME}
	    private var _${PROPERTY}: ${DATA_TYPE};
${SEPARATOR}
	    /**
	     * ${PROPERTY_NAME}
	     *
	     * @remark ${REMARK}
	     * @returns {${DATA_TYPE}}
	     */
	    public function get ${PROPERTY}(): ${DATA_TYPE} {
	        return this._${PROPERTY};
	    }